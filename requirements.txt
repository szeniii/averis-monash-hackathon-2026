# Versions taken from the working local environment, not guessed.
# Regenerate with:  .venv/bin/pip freeze | grep -iE '^(pdfplumber|python-docx|openpyxl|google-genai|fastapi|uvicorn)='

# Field extraction from the four attachment formats
pdfplumber==0.11.10       # PDF text
python-docx==1.2.0        # .docx
openpyxl==3.1.5           # .xlsx

# AI: classifies the emails rules cannot settle, and reads the PDF layouts
# no deterministic parser can handle
google-genai==2.24.0

# Review interface
fastapi==0.141.1
uvicorn==0.53.0
python-multipart==0.0.20  # file uploads in the triage app

# Dev only
pytest==8.3.4
