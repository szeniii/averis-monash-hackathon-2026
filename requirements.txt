# Field extraction from the four attachment formats
pdfplumber==0.11.4        # PDF text
python-docx==1.1.2        # .docx
openpyxl==3.1.5           # .xlsx

# AI: classification of ambiguous emails, and reading the PDF layouts
# no deterministic parser can handle
google-genai==1.2.0

# Review interface
fastapi==0.115.6
uvicorn==0.34.0

# Reads GEMINI_API_KEY from a .env file in local development.
# On Render the key comes from the dashboard's environment variables instead.
python-dotenv==1.0.1
